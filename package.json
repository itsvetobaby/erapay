{
  "name": "ERAPAY",
  "version": "0.1.0",
  "author": "Libens Veto",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^7.4.0",
    "http-server": "^0.12.1"
  },
  "scripts": {
    "start": "echo GO FORTH AND BUILD 'ERAPAY: http://localhost:8080'; npx http-server -c-1  src --proxy http://localhost:8080?",
    "lint": "eslint src/js/*.js --fix;eslint src/js/components/*.js --fix; eslint src/js/views/*.js --fix",
    "build": "preact build"
  }
}
